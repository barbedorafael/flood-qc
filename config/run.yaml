# Overrides operacionais do dia.
# Este arquivo pode ficar vazio quando não houver ajustes.

run:
  mode: "operational"
  reference_time: null
  event_name: null

# Exemplo para usar replay:
# run:
#   mode: "event_replay"
#   reference_time: "2024-05-05T09:00:00"
#   event_name: "maio_2024"
